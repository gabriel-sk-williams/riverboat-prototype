{
  "name": "riverboat",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@emotion/react": "^11.8.1",
    "@emotion/styled": "^11.8.1",
    "@mui/icons-material": "^5.5.1",
    "@mui/material": "^5.5.0",
    "@testing-library/jest-dom": "^5.16.2",
    "@testing-library/react": "^12.1.3",
    "@testing-library/user-event": "^13.5.0",
    "axios": "^0.26.0",
    "neo4j-driver": "^4.4.3",
    "react": "^17.0.2",
    "react-bootstrap": "^2.2.0",
    "react-dom": "^17.0.2",
    "react-scripts": "5.0.0",
    "uuid": "^8.3.2",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "reset": "node -e require('./attack').reset()",
    "refresh": "node -e require('./attack').refresh()",
    "player": "node -e require('./attack').mergePlayers()",
    "event": "node -e require('./attack').mergeEvent()",
    "circle": "node -e require('./attack').mergeCircle()",
    "space": "node -e require('./attack').mergeSpace()",
    "model": "node -e require('./attack').mergeModel()",
    "dc": "node -e require('./attack').detachCircle()",
    "dr": "node -e require('./attack').detachRelationships()",
    "activate": "node -e require('./attack').activate()",
    "calc": "node -e require('./attack').serveCalc()",
    "testq": "node -e require('./attack').testQuery()"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
